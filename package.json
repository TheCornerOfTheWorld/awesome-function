{
  "name": "awesomefunction",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "server": "pnpm -r --filter=./packages/server run dev",
    "client": "pnpm -r --filter=./packages/client run dev",
    "dev": "pnpm -r run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=14",
    "pnpm": ">=7"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}